{
  "name": "@nodeneo/mcp-server",
  "version": "0.1.0",
  "description": "MCP server for Morpheus AI models via NodeNeo gateway — keeps all prompts local",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
