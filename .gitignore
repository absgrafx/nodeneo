# Go
go/vendor/

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.iml

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
*.app/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/

# iOS
ios/Pods/
ios/.symlinks/

# Android
android/.gradle/
android/local.properties
*.apk
*.aab

# Cursor IDE (per-developer config, may contain secrets) — except shared
# project rules under .cursor/rules/ which we ship to all contributors.
.cursor/*
!.cursor/rules/

# Secrets
*.env
*.key
*.pem

# App Store Connect API key — created in ASC → Users & Access → Integrations,
# downloadable exactly once. Treat like a private key; never commit.
*.p8
AuthKey_*.p8
.appstore

# Internal working docs — drafts, security plans, handoff context, dev
# roadmaps. Treat as ephemeral planning notes; never publish. Public
# README.md / docs go elsewhere.
.ai-docs/
